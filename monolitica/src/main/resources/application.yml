server:
  port: 8090

spring:
  application:
    name: proyecto_top_education
  jpa:
    show-sql: true
    hibernate:
      dialect: MySQL8Dialect
      ddl-auto: create
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    dbname: topeducation
    url: jdbc:mysql://localhost:3306/topeducation
    username: root
    password: admin
